import * as types from './types'

export const update = () => ({
  type: types.UPDATE,
})

export const clear = () => ({
  type: types.CLEAR,
})

export const dismiss = () => ({
  type: types.DISMISS,
})
