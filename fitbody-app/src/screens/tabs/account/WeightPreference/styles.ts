import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
  },
  slide: {
    flex: 1,
    paddingTop: 5,
  },
  optionButton: { marginTop: 10, marginBottom: 10 },
})

export default styles
